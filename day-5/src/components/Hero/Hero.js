import React from 'react';
import {HeroContainer, HeroText} from './Hero.styled';

const Hero = () => (
  <HeroContainer>
    <HeroText>Welcome to my website</HeroText>
  </HeroContainer>
);

export default Hero;